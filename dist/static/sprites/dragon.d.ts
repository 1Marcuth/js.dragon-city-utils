import BaseStaticDownloader from "../base";
declare class DragonSprite extends BaseStaticDownloader {
    constructor(imageName: string, phase: number, skin?: number | null, imageQuality?: number);
}
export default DragonSprite;
