import BaseStaticDownloader from "../../base";
declare class DragonSpineAnimation extends BaseStaticDownloader {
    constructor(imageName: string, phase: number, skin?: number | null, platform?: string, useNewUrl?: boolean);
}
export default DragonSpineAnimation;
