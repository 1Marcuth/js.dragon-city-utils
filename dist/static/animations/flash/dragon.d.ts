import BaseStaticDownloader from "../../base";
declare class DragonFlashAnimation extends BaseStaticDownloader {
    constructor(imageName: string, phase: number, skin?: number | null);
}
export default DragonFlashAnimation;
