declare function calculateStrongs(elements: string[]): string[];
declare function calculateWeaknessses(firstElement: string): string[] | [];
declare const _default: {
    calculateStrongs: typeof calculateStrongs;
    calculateWeaknessses: typeof calculateWeaknessses;
};
export default _default;
